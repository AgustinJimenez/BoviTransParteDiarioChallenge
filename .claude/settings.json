{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"\nimport json, sys, pathlib\ndata = json.load(sys.stdin)\npath = data.get('tool_input', {}).get('file_path', '')\nskip = ['/integration/', '/__tests__/', '.test.', '.spec.', 'messages/', 'DOCUMENTACION', 'BACKLOG', 'TEST_PLAN', 'README', 'questions', 'settings.json']\nif path.endswith(('.ts', '.tsx')) and not any(s in path for s in skip):\n    pathlib.Path('/tmp/bovitrans_code_changed').touch()\n\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"\nimport pathlib\nflag = pathlib.Path('/tmp/bovitrans_code_changed')\nif flag.exists():\n    flag.unlink()\n    print('Source code was modified this turn. Before finishing: (1) do unit or integration tests need adding/updating? (2) do DOCUMENTACION.md, BACKLOG.md or TEST_PLAN.md need updating? (3) run npm run lint && npm run type-check to catch regressions.')\n\""
          }
        ]
      }
    ]
  }
}
